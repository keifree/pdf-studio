# PDF Studio Project Handover (v8.0.2)

新しいAIアシスタントへ：このファイルは、以前のセッションからの引き継ぎ資料です。ユーザーからの指示があった場合は、まずこのファイルを読んで現在のプロジェクトの仕様と背景を把握してください。

## 1. プロジェクト概要
* **アプリ名**: Antigravity PDF Studio
* **主な機能**: Google Driveと連携してPDFをブラウザ上で直接読み書きできるPWA（Progressive Web App）。複数タブ対応、右綴じ（漫画・和書向け）サポート、手書き・図形・テキストなどのアノテーション（注釈）機能を持つ。
* **技術スタック**: HTML / Vanilla JS / CSS.
  * PDF描画エンジン: `pdf.js` (Mozilla)
  * PDF出力・編集エンジン: `pdf-lib`

## 2. コアファイルの構成と役割
* `index.html`: アプリのUIレイアウト、PWAのメタデータ設定、ツールバーUIなど。
* `style.css`: UIのスタイリング。
* `sw.js`: PWAのService Worker。`Network-First`戦略でキャッシュを管理。
* `js/app.js`: アプリ全体の状態管理、UIイベントのバインディング、Drive連携とエクスポートのトリガー。
* `js/pdf-viewer.js`: `pdf.js`を用いたPDFのレンダリング、見開き表示、ページ移動機能。
* `js/annotation-manager.js`: ユーザーが画面上に書き込む線や図形、テキストのCanvasベースのUI描画とデータ管理。
* `js/pdf-exporter.js`: `pdf-lib`を用いて、保存時にPDFファイル自体にアノテーションを焼き付ける（ベイクする）処理。
* `js/drive-manager.js`: Google Drive APIを利用したファイルの読み書き。

## 3. 最近の重要な修正とアーキテクチャの変更 (v8.0.0 - v8.0.2)
新しいエージェントがコードを触る上で**絶対に知っておくべき**仕様です。

### A. 座標の正規化（パーセンテージベース化）
**問題**: 以前はiPadとスマートフォンなど異なる画面解像度で線を引くと、デバイス間で描画位置や線の太さがズレるバグがありました。
**解決策**: `annotation-manager.js` および `pdf-exporter.js` を改修し、アノテーションの座標（X, Y）やサイズをすべて**「PDFページ幅・高さに対する割合（0.0〜1.0）」**で保存・描画するように完全リファクタリング済みです。

### B. テキスト・引出線の書き出し時エラー（winansiエンコード問題）
**問題**: `pdf-lib`のデフォルトフォントが日本語などのUnicodeに対応していないため、日本語テキストをPDFに保存しようとするとクラッシュしていました。
**解決策**: 重い日本語フォントファイルを読み込むことを避け、`pdf-exporter.js` 内の `textToImage` メソッドを用いて、**テキストをブラウザ上で一時的な高画質透過PNG画像に変換し、それをPDFに画像として埋め込む**手法を採用しています。これにより全言語・絵文字に完全対応しています。

### C. 「焼き付け（ベイク）」の仕様と消しゴムの制約
**仕様**: Googleドライブへの上書き保存、またはPDFのダウンロードを実行すると、描画されたアノテーションはすべて**PDFのベース画像として完全に焼き付け（Flatten）**られます。これはiPhone標準プレビューやAcrobat等、外部のあらゆるPDFリーダーで確実に見えるようにするための必須仕様です。
**制約**: 焼き付けが行われると、それは「編集可能な注釈レイヤー」ではなく「PDFの元々の絵の一部」になります。そのため、**一度保存した後に開き直した場合、消しゴムツールで消すことはできません**。また、左サイドバーの「注釈レイヤー表示/非表示」機能も、**保存前の編集中レイヤーに対してのみ有効**です。

## 4. ユーザー環境について
* **GitHub Desktopを使用**: ユーザーは現在作業中のローカルディレクトリ（scratchフォルダ）から、自身がGitHub Desktopで管理しているフォルダへ手動でファイルをコピーしてPushしています。そのため、AIが直接 `git` コマンドでPushすることはできません。修正後は「GitHub DesktopのフォルダにコピーしてPushしてください」と案内してください。
* **PWAキャッシュの注意**: `index.html` と `sw.js` のキャッシュバージョン（例: `v8.0.2`）が更新されないと、iOSやiPadOSのSafariが古いJSファイルを読み込み続ける可能性があります。JSを大きく修正した場合は、必ずバージョン文字列をインクリメントしてください。

## 5. 自己完結型 編集可能PDF（OCGレイヤー方式）の実装 (v8.0.2)
* アノテーションをPDFへ「焼き付け（Bake）」する際、それらをすべて `AntigravityLayer` という名称の OCG（Optional Content Group）レイヤーとしてPDFへ書き込んでいます。
* さらに、再編集用のJSONデータを `antigravity_annotations.json` としてPDF内部に添付ファイルとして埋め込んでいます。
* 当アプリでPDFを読み込む際、JSONデータを抽出してキャンバスへ復元すると同時に、`pdf.js` に対して `AntigravityLayer` を非表示設定としてレンダリングさせています。これにより、見た目上の完全な編集/消去と、外部アプリでの表示互換性を両立させています。

## 5. 今後の対応可能な拡張アイデア（ユーザーの希望次第）
* もしユーザーが「保存後も消しゴムで消せるようにしたい」と強く希望した場合、PDFへ焼き付けるのではなく、`AnnotationManager` のJSONデータをGoogle Driveのアプリプロパティ等に分離して保存する、などの大幅なアーキテクチャ変更（別レイヤーアプローチ）が必要になります。
